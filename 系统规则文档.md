个人作品集官网开发・系统规则文档
本文档为项目最高约束准则，所有输出、代码、方案必须严格遵守，违反任一强制条款均视为无效输出。
适用场景：可直接粘贴至任意大模型（Cursor / Claude / GPT 等）对话开头作为全局系统规则，统一开发标准，防止技能滥用与代码混乱。
一、项目总纲
本项目为纯前端 React 个人作品集单页网站（SPA），无后端依赖，支持静态部署。
核心目标：打造具有强个人辨识度、华丽且克制的动态视觉效果；同时保证架构清晰、职责边界明确，支持长期持续迭代维护，杜绝代码随迭代逐渐失控。
基础技术栈（唯一允许使用的底层技术，版本锁定）

| 类别 | 技术 | 锁定版本 | 说明 |
|------|------|----------|------|
| 框架 | React + TypeScript | 19.2.8 + 7.0 | 函数式组件 + Hooks，TypeScript 原生编译 |
| 样式 | TailwindCSS | 4.3.3 | 原子化 CSS，CSS-first 配置 |
| 构建 | Vite | 8.1.4 | Rolldown 打包引擎 |
| 组件库 | shadcn/ui | latest | 基于 Base UI / Radix UI，源码级可定制 |
| 2D 动画 | Anime.js | 4.5.0 | 创意时序动画引擎 |
| 3D 引擎 | Three.js | 0.184.0 | WebGL 背景装饰层 |
| 包管理 | pnpm | latest | 推荐 |

> **版本锁定铁则**：以上版本为 2026-08 确认的最新稳定版。大模型不得自行"升级"版本或使用训练数据中已过时的 API。如因时间推移需更新版本，必须先修改本文档中的版本号，再使用新版本 API。
❗ 严禁未经许可额外引入任何 UI 组件库、动画库、状态管理库。
二、可用资源清单 & 严格职责边界

> **重要说明**：以下资源分为三类——① Trae IDE 内置 Skill（可直接调用）、② 本地 Skills 资源（位于 `d:\MyBlog\Skills\`，需读取后作为上下文使用，不可作为工具调用）、③ npm 代码库（通过包管理器引入项目）。三类资源的调用方式完全不同，严禁混淆。

---

### 1. Trae IDE 内置 Skill（可直接通过 Skill 工具调用）

以下 Skill 在当前 Trae IDE 环境中实际存在，可以且仅可以通过 `Skill` 工具调用：

| Skill 名称 | 专属职责 | 严禁行为 |
|-----------|----------|----------|
| `frontend-design` | 创建高质量前端界面。负责页面视觉风格、布局架构、组件设计、设计规范输出。**本项目的核心执行 Skill。** | 实现复杂 3D 场景、编写后端代码 |
| `frontend-code-review` | 前端代码审查。检查代码质量、最佳实践、性能问题。**在阶段三完成后使用。** | 替代日常开发、修改视觉风格 |
| `webapp-testing` | 基于 Playwright 的 Web 应用测试。负责浏览器预览、截图、多尺寸响应式测试、控制台日志采集。**仅用于阶段四验收。** | 开发过程中频繁调用；用于辅助编写代码 |
| `skill-creator` | 创建新的 Skill。**仅在需要创建新技能时使用，与日常开发无关。** | 在正常开发流程中调用 |

> **铁则**：以上 4 个是当前环境唯一可用的 Skill。调用前必须确认 Skill 名称完全匹配。**严禁调用任何未列出的 Skill 名称。**

---

### 2. 本地 Skills 资源（位于 `d:\MyBlog\Skills\`，不可作为工具调用）

以下资源已下载到本地，大模型应**读取其内容作为设计/编码上下文**，但**不能作为 Skill 工具调用**：

| 资源文件夹 | 核心文件 | 逻辑角色 | 使用方式 |
|-----------|----------|----------|----------|
| `taste-skill-main` | `skills/taste-skill/SKILL.md` | 视觉审美总控 — 制定色彩系统、字体层级、留白网格、光影质感；通过"三 Dial 系统"（VARIANCE / MOTION / DENSITY）量化设计方向 | 读取 SKILL.md，将其设计原则作为上下文约束所有视觉输出 |
| `ui-ux-pro-max-skill-main` | `skill.json`（v2.13.0） | 页面架构与布局规范 — 84 种 UI 风格、192 套色彩方案、74 组字体搭配、98 条 UX 准则 | 读取其设计数据作为布局参考；可选执行 `npx ui-ux-pro-max-cli init --ai trae` |
| `impeccable-main` | `.trae/skills/impeccable/SKILL.md`（v4.0.4） | 精细化微交互与滚动叙事 — 23 条设计命令、59 条反模式检测规则 | 读取 SKILL.md 作为设计质量标尺；可选执行 `npx impeccable install` |
| `andrej-karpathy-skills-main` | `CLAUDE.md` | 代码质量约束 — 四原则：Think Before Coding / Simplicity First / Surgical Changes / Goal-Driven Execution | 将 CLAUDE.md 四原则作为编码规范的底层约束 |
| `react-bits-main` | `content/`（160+ 组件） | DOM 层动效组件库 — 36 种交互动效、52 种背景特效、44 种 UI 组件、32 种文字动效 | 从 `ts-tailwind/` 目录复制需要的 TS+Tailwind 组件到项目 `src/components/animation/` |
| `playwright-mcp-main` | `package.json`（v0.0.79） | 浏览器自动化验收测试 | 配置为 MCP Server；当前环境已有 `integrated_browser` MCP 和 `webapp-testing` Skill 可替代 |

> **铁则**：以上资源是**本地文件**，不是可调用的工具。大模型必须通过 `Read` 工具读取其内容，**严禁使用 `Skill` 工具调用这些名称**（如 `taste-skill-main`、`impeccable-main` 等不可作为 Skill 名称调用）。

---

### 3. npm 代码库（通过包管理器引入项目）

| 代码库 | 版本 | 专属职责 | 严禁行为 |
|--------|------|----------|----------|
| Anime.js | 4.5.0 | 高度定制化 2D 创意动画。仅用于 SVG 路径描边 / 形变、多元素复杂时序联动、数字流动、创意加载动画、首页标题特效。必须封装为 React 自定义 Hook，严格管理生命周期。本地源码位于 `d:\MyBlog\Skills\Component\anime-master\`。 | 用于普通 DOM 悬浮 / 入场动画、用于 3D 场景控制、不做实例销毁 |
| Three.js | 0.184.0 | WebGL 三维视觉效果。仅用于 Hero 区域 3D 粒子 / 星云背景、空间视差、装饰性 3D 元素。仅作为背景装饰层，不承载核心 UI 信息。 | 实现 DOM 界面、实现普通 2D 动画、承载核心内容 |
| shadcn/ui | latest | UI 组件库。提供按钮、卡片、导航栏等基础原子组件，通过 CLI 按需添加，源码直接落入项目 src/components/ui/，可自由定制修改。 | 直接修改 node_modules 内源码、替代 Anime.js/Three.js 实现动画效果 |
| react-bits 组件 | 从本地 `Skills/react-bits-main/ts-tailwind/` 复制 | DOM 层动效组件。按需复制所需组件（如 Magnet、SplashCursor、BlurText 等），放入项目 `src/components/animation/` 目录。 | 全量复制整个库、修改组件内部核心逻辑、用于 3D 场景 |

> **react-bits 使用规范**：开发时先查阅 `d:\MyBlog\Skills\react-bits-main\content\` 目录找到所需组件，从 `ts-tailwind/` 对应路径复制 TS+Tailwind 版本到项目。每次只复制当前需要的组件，禁止全量复制。
三、动效分层铁律（最高优先级，绝对禁止违反）
全站动效严格分为三层，各层职责完全隔离，禁止多个动画引擎同时控制同一目标元素。
DOM UI 动效层
执行者：react-bits 组件（从本地 Skills 复制）+ impeccable 设计原则（读取 SKILL.md 作为质量标尺）
作用对象：所有 DOM 页面元素、按钮、卡片、文字段落
负责：滚动入场、悬浮反馈、基础过渡、页面叙事
2D 创意动画层
执行者：Anime.js
作用对象：SVG 图形、数字、创意标题、加载动画
负责：高度定制化的时序动画、路径动画
WebGL 三维层
执行者：Three.js
作用对象：Canvas 画布
负责：3D 背景、粒子空间、视差场景
四、开发迭代工作流程（强制遵守）
核心原则：PRD 先行，增量开发，禁止全量重写
任何需求变更，第一步必须更新 PRD 文档
用户提出修改需求后，先输出更新后的完整 PRD + 变更摘要
必须经用户确认 PRD 无误后，方可进入代码开发阶段
严禁跳过文档直接改代码
代码修改必须为增量式
只输出发生变动的文件、组件代码
完整保留未改动部分的原有逻辑与结构
严禁以 "优化"" 重构 " 为名全量重写整个项目 / 页面
标准四阶段开发顺序
阶段一：输出 / 更新 PRD 需求文档（不写代码）
阶段二：基于 PRD 输出页面骨架与基础 UI（无复杂动效）
阶段三：分层叠加动效（先 DOM 动效 → 再创意动画 → 最后 3D 场景）
阶段四：调用 webapp-testing Skill 或 integrated_browser MCP 进行验收测试
五、代码工程强制规范
1. 标准目录结构
plaintext
src/
├── components/
│   ├── ui/          # 基础原子组件（按钮、标签、卡片等）
│   ├── animation/   # 动画封装组件与自定义 Hook
│   └── sections/    # 页面板块组件（Navbar、Hero、About 等）
├── hooks/           # 通用自定义 Hook
├── styles/          # 全局样式与 Tailwind 主题配置
├── utils/           # 工具函数
├── assets/          # 静态资源（图片、图标）
├── App.tsx
└── main.tsx
2. 组件与编码规则
全部使用函数式组件 + Hooks，遵循单一职责原则
通用组件通过 props 配置，禁止硬编码业务内容
动画逻辑必须从渲染组件中抽离为独立 Hook
TypeScript 类型完整，禁止滥用 any
3. 动画生命周期铁则
Anime.js 实例、Three.js 场景必须在 useEffect 清理函数中完整销毁
组件卸载时必须移除所有事件监听、取消所有动画帧
严禁出现动画实例叠加、内存泄漏
六、视觉与交互统一标准
风格基调：暗黑科技极简风，克制高级，拒绝廉价霓虹感与过度闪烁
色彩系统：深色基底 + 科技蓝主色 + 低饱和辅助色，全局统一变量管理
排版系统：严格字体层级，8px 网格留白体系
交互原则：
所有可交互元素必有明确的 hover /active 状态反馈
动效服务于信息传达，不为炫技干扰阅读
全局统一缓动曲线，动效节奏一致
七、性能与兼容性红线
桌面端滚动帧率不低于 55fps
动画优先使用 transform / opacity，避免频繁重排重绘
强制降级策略：
尊重系统 prefers-reduced-motion 设置，开启减弱动画时关闭非必要动效
低性能设备自动关闭 Three.js 场景，替换为静态渐变背景
移动端默认降低动效强度，保证滚动流畅
响应式适配 375px ~ 1920px+ 全尺寸

使用方法
将本文档完整粘贴到大模型对话的最开头，声明为「全局系统规则，全程遵守」
随后再提出具体需求（例如：生成第一版 PRD、实现 Hero 区域、新增项目卡片动效等）
每次新开对话都先粘贴本规则，保证所有输出遵循同一套标准

⚠️ 环境适配提醒
当前环境为 Trae IDE，内置 4 个可用 Skill：frontend-design、frontend-code-review、webapp-testing、skill-creator
本地 Skills 资源位于 `d:\MyBlog\Skills\`，大模型通过 Read 工具读取其内容作为上下文，不可通过 Skill 工具调用
如切换到其他 AI 环境（Cursor/Claude/GPT），需重新确认该环境的可用 Skill 列表，并更新本文档的资源清单
环境搭建指南详见 `d:\MyBlog\环境搭建指南.md`